[project]
name = "agent-report-demo"
version = "0.1.0"
description = "LangGraph report agent demo with LangChain tools"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0,<2.0",
    "langgraph>=1.0,<2.0",
]